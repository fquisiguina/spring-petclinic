#!groovy
pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh '''
                #!/bin/bash
                echo "Usando bash en lugar de sh"
                '''
            }
        }
    }
}

